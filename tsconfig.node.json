{
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        "composite": true,
        "lib": ["ESNext"],
        "types": ["node"],
        "skipLibCheck": true
    },
    "include": ["builds/**/*", "builds/**/*.json", "gulpfile.ts"],
    "exclude": ["**/dist"]
}
